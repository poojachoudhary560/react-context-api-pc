import React from 'react';
import CustomerInfo from './CustomerInfo';

const Header = (props) => {
  return (
    <>
      <header>
        <CustomerInfo />
      </header>
    </>
  );
};

export default Header;
